[package]
name = "application-reporting"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
domain-accounts = { path = "../../domain/accounts" }
domain-orders = { path = "../../domain/orders" }
