# VCM:BEGIN version=1
# VCM runtime task metadata, handoffs, session records, logs, and task worktrees.
# This example keeps .ai/vcm/ visible so readers can inspect runtime handoff artifacts.
# .ai/vcm/
.claude/worktrees/
# VCM:END

/target/
