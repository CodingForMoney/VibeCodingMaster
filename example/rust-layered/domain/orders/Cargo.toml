[package]
name = "domain-orders"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
domain-catalog = { path = "../catalog" }
foundation-identity = { path = "../../foundation/identity" }
